services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - ./packages/server/.env
    restart: unless-stopped
    volumes:
      - ./packages/server/downloads:/app/packages/server/downloads
